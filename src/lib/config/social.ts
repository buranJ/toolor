/**
 * Brand social links. Replace the placeholder profile URLs with the real
 * TOOLOR accounts before launch — these are the single source of truth for
 * the footer icons and the Instagram call-to-action on the homepage.
 */
export const socialLinks = {
  instagram: "https://www.instagram.com/",
  tiktok: "https://www.tiktok.com/",
  whatsapp: "https://wa.me/",
} as const;

/** Handle shown next to the Instagram call-to-action. */
export const instagramHandle = "@toolor_official";
